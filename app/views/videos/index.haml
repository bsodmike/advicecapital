%h2 Video-arkiv

- @videos.each do |video|
	.video
		%video#movie{:controls => "controls", :height => "240", :width => "320"}
			%source{:src => video.url, :type => "video/mp4"}

		=# debug video
		- if can? :manage, video
			.notice.actions
				= link_to 'Slet', video_path(video.video_id), :confirm => 'Er du sikker?', :method => :delete