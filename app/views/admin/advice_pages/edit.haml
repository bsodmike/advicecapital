%h3 Rediger side

= form_for [:admin, @page] do |f|
  .field
    = f.label :title, "Titel:"
    %br/
    = f.text_field :title
  .field
    = f.label :content, "Indhold:"
    %br/
    = f.text_area :content
  .action
    = f.submit "Gem"
