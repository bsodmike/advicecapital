%h3 Rediger side

= form_for [:admin, @page] do |f|
  .field
    = f.label :title, "Titel:"
    = f.text_field :title
  .field
    = f.label :content, "Indhold:"
    = f.text_area :content
  .action
    = f.submit "Gem"
