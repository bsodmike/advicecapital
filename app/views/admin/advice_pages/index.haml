%h3 Sider

#table-wrapper
  %table
    - @pages.each do |r|
      %tr
        %td= r.title.empty? ? "No Title Set" : r.title
        %td= r.slug
        %td= r.template
        %td
          = link_to "Rediger", edit_admin_advice_page_path(r)
          | 
          = link_to "Vis", r.slug, :target => "_blank"