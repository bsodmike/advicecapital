#main_content
  #slider.nivoSlider
    
    //.arrow_left.arrows
    //.arrow_right.arrows
    %ul
      %li= image_tag 'header-1.jpg'
      %li= image_tag 'IMG_0940.jpg'


  #content
    .span-12{:style=>"margin-bottom:15px;"}
      %h3 Advice Capital A/S
      .news_ticker
        %ul
          - @news.each do |news|
            %li
              %b= news.created_at.strftime('%d/%m/%y - %H:%M:%S')
            %li
              = raw(truncate(news.content, :length => 50))
    .span-9
      %h3 TEST

    .span-9
      %h3 Nyheder fra Advice Capital/Advice Invest

      Nyhed 1
      %br/
      %br/
      Nyhed 2
      %br/
      %br/
      Nyhed 3
      
.stay-clear
#bottom
  #stocks
    %ul
      =# render :partial => "default_ystock"
      - Resque.enqueue(FetchStocks)
      = render :partial => "resque_ystock"

