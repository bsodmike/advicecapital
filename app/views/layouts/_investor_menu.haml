/- investor_id = current_investor(current_user)
.investor_box
  %div{:style=>"float:left;"}
    %ul
      %li= link_to 'Investor Profil', admin_investor_path(current_user.investor_id) unless current_user.investor_id.nil?
      %li= link_to 'Events', admin_events_path
      //%li= link_to 'Brugerindstillinger', edit_admin_user_path(current_user)
      %li= link_to "Log ud", destroy_user_session_path, :method => :delete
